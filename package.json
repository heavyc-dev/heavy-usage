{
  "name": "heavy-usage",
  "version": "1.3.1",
  "description": "Claude Code usage guard + loop wind-down — reads the official rate-limit numbers. Pure Node, no deps.",
  "license": "MIT",
  "private": true,
  "homepage": "https://github.com/heavyc-dev/heavy-usage",
  "scripts": {
    "test": "node tests/run.js",
    "check-version": "node scripts/check-version-sync.js",
    "bump": "node scripts/bump.js"
  }
}
